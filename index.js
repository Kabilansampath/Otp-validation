// Import stylesheets
import './style.css';

function valid(x) {
  var y = x.value;
  if (y && y.length == 1) {
    var num = parseInt(x.id);
    var nextnum = num + 1;
    var z = document.getElementById(nextID.toString());
    z.focus;
  }
}
